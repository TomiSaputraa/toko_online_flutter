name: toko_online
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  http: ^0.13.5
  # wajib tambahkan untuk json serializable
  json_annotation: ^4.6.0

dev_dependencies:
  # wajib tambahkan untuk json serializable
  build_runner: ^2.0.0
  json_serializable: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
